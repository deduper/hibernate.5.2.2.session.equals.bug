# hibernate.5.2.2.session.equals.bug
Demonstrates that SessionImpl.equals(Object) incorrectly implements the behavior that the Java Language Specification prescribes for overriding Object.equals(Object).
